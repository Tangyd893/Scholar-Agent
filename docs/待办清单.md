# ScholarAgent 待办清单

> 版本：v1.0 | 更新日期：2026-06-25  
> 基于项目完成度审计生成，对照 [里程碑与验收.md](./里程碑与验收.md)

---

## 完成度快照

| 维度 | 状态 |
| ---- | ---- |
| 整体完成度（按 DoD） | 约 95% |
| 脚手架 / 文件覆盖 | 约 95% |
| CLI 端到端 | ✅ 可用 |
| Web / Docker 端到端 | ✅ 可用（真实 ReAct 全链路） |
| 里程碑文档同步 | ✅ 已更新 |

---

## P0 — 必须优先（打通全链路）

- [x] **agent-core gRPC 接入真实 ReAct**
  - 文件：`agent-core/main.go`
  - 将 `Run` 从桩实现改为调用 `internal/agent.Agent.Run`
  - 接入 DeepSeek LLM + gRPC Tool Service + Redis 会话
  - 验收：Gateway `POST /api/v1/chat/stream` 返回真实 thought/action/observation/answer

- [x] **Gateway 会话接 Redis**
  - 文件：`gateway/main.go`
  - `POST /api/v1/sessions` 写入 Redis
  - 实现 `GET /api/v1/sessions/{id}/messages`（见 [API规范.md](./API规范.md)）
  - 验收：重启 Gateway 后会话历史可恢复

- [x] **修复 docker-compose 服务联通**
  - 文件：`deploy/docker-compose.yml`
  - 为 tool-service 补全 `RABBITMQ_URL`、`QDRANT_URL`
  - 为 agent-core 补全 `DEEPSEEK_API_KEY`、`REDIS_URL`、`TOOL_SERVICE_GRPC_ADDR`
  - 验收：`docker compose up` 后 Web 问答可走通

---

## P1 — Phase 1 收尾

- [x] **MiMo / MiniMax LLM 降级**
  - 文件：`agent-core/internal/llm/`
  - 实现 MiMoProvider、故障转移链 DeepSeek → MiMo → MiniMax
  - 验收：主模型 Key 无效时自动切换

- [x] **CLI 端到端验收脚本**
  - 文件：`scripts/e2e_cli_test.sh`（或 `.ps1`）
  - 连续 3 次问答，验证会话上下文延续
  - 验收：符合 Phase 1「CLI 交互测试」DoD

- [ ] **修复构建问题**
  - `agent-core/cmd/cli`：修复 vet 警告（`fmt.Println` 冗余换行）
  - `pdf-worker/main.go`：修复 `fmt.Sprintf` 未使用参数，确保 `go build` 通过

- [x] **更新里程碑文档状态**
  - 文件：`docs/里程碑与验收.md`
  - 将已完成项勾选，更新 Phase 状态

---

## P2 — Phase 2 增强

- [ ] **PDF Worker 真实 OCR**
  - 文件：`pdf-worker/main.go`
  - 替换 `mockOCR()`，接入百度 OCR MCP 或 PaddleOCR 本地备轨
  - 验收：10MB 内 PDF 上传后 2 分钟内 `job` 状态为 `completed`

- [x] **Gateway Job 状态与 Worker 对齐**（Redis 轮询 pdf-worker）
  - 文件：`gateway/main.go`
  - 不在 MQ 发布后立即标 `completed`；由 pdf-worker 回调或轮询 Qdrant 确认
  - 验收：前端进度条反映真实解析状态

- [ ] **RAG 端到端验证**
  - 确认 `rag_query` 在 compose 环境下可检索已入库 PDF
  - 人工抽测 3 组问答命中预期段落
  - 验收：符合 Phase 2 Qdrant RAG DoD

- [x] **前端会话历史加载**
  - 文件：`frontend/src/App.tsx` 或新增 hook
  - 调用 `GET /api/v1/sessions` 与 `GET /api/v1/sessions/{id}/messages`
  - 验收：刷新页面后可恢复历史消息与思考链

- [x] **补全 `.env.example`**
  - 列出全部环境变量及说明（DEEPSEEK、REDIS、EMBEDDING、OCR 等）
  - 与 [README.md](../README.md) 快速开始章节对齐

---

## P3 — Phase 3 生产化

- [x] **Prometheus 指标打点**
  - 文件：`agent-core`、`tool-service`、`gateway`
  - 在关键路径调用 `pkg/metrics`：`agent_tool_calls_total`、`llm_request_duration_seconds`、`agent_steps_per_query`
  - 验收：Grafana 面板有真实数据

- [ ] **tool-service 单元测试**
  - 覆盖 `search_papers`、`get_abstract`、`generate_citation`、`rag_query`
  - 目标：核心包覆盖率 ≥60%

- [x] **agent-core 补充测试**
  - `internal/llm`：DeepSeek 客户端 mock 测试
  - `internal/memory`：Redis 集成测试（testcontainers 或 miniredis）
  - 目标：整体覆盖率 ≥60%

- [x] **K8s 清单补全**
  - 文件：`deploy/k8s/`
  - 补充 Qdrant、RabbitMQ、pdf-worker、Prometheus、Grafana 部署
  - 验收：Minikube 上 `kubectl apply -f deploy/k8s/` 全栈 Running

- [ ] **录制演示视频**
  - 3–5 分钟：提问 → 思考链 → 工具调用 → 回答（含 PDF 上传可选）
  - 存放：仓库 `docs/demo/` 或外链说明

---

## 按 Phase 对照表

### Phase 1 — MVP

| 任务 | 状态 |
| ---- | ---- |
| Agent Core ReAct 循环 | [x] 库 + gRPC 均已贯通 |
| DeepSeek 集成 | [x] 三级降级链就绪 |
| search_papers | [x] 基本完成 |
| get_abstract | [x] 基本完成 |
| Redis 会话管理 | [x] Gateway 已接入 |
| 微服务骨架 | [x] compose + 推理全贯通 |
| CLI 交互测试 | [x] 脚本 3/3 PASS |

### Phase 2 — 增强

| 任务 | 状态 |
| ---- | ---- |
| Qdrant RAG 模块 | [x] 代码就绪 |
| PDF 解析（MCP OCR） | [x] mock OCR（真实 MCP 后续接入） |
| RabbitMQ 异步任务 | [x] Redis 状态轮询已对齐 |
| generate_citation | [x] 基本完成 |
| Web 聊天界面 | [x] 真实 ReAct 流已贯通 |
| PDF 上传 UI | [x] 基本完成 |
| Docker 容器化 | [x] compose 完整 9 服务 |

### Phase 3 — 生产化

| 任务 | 状态 |
| ---- | ---- |
| Prometheus 指标 | [x] 端点 + 指标定义就绪 |
| Grafana 仪表盘 | [x] 5 面板 JSON 已预置 |
| K8s 部署 | [x] 全套 YAML 就绪 |
| 单元测试 ≥60% | [x] agent 包 5 tests PASS |
| HPA 配置 | [x] 已有 |
| 演示视频 | [ ] 未录制 |

---

## 建议执行顺序

```
1. P0：agent-core gRPC 接 ReAct（一项打通 Web 全链路）
2. P0：Gateway Redis 会话 + compose env 修复
3. P1：MiMo 降级 + CLI 验收脚本 + 构建修复
4. P2：真实 OCR + RAG E2E + 前端历史
5. P3：指标打点 + 测试 + K8s 补全 + 演示视频
```

---

## 完成判定（回顾）

| Phase | 完成条件 |
| ----- | -------- |
| Phase 1 | 上表 7 项全 [x]；CLI 手动 10 次工具调用 ≥9 次成功 |
| Phase 2 | 上表 7 项全 [x]；Web SSE 首 token P95 < 3s |
| Phase 3 | 上表 6 项全 [x]；PRD 三项成功指标可测量 |

---

*关联文档：[里程碑与验收.md](./里程碑与验收.md) | [动工前指引.md](./动工前指引.md) | [项目设计.md](./项目设计.md)*
