{
  "apiVersion": 1,
  "providers": [
    {
      "name": "ScholarAgent",
      "folder": "",
      "type": "file",
      "options": {
        "path": "/etc/grafana/provisioning/dashboards"
      }
    }
  ]
}
